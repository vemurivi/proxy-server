<div class="outerContainer">
  <div class="contentContainer">
    <div class="tableContainer">

      <!-- Header Section -->
      <div class="tableRow" [class.leftShadow]="isScrolled" (scroll)="scroll($event)">
        <!-- Header Cell for Planned Percent Volume -->
        <div class="header-cell-1" 
             [class.hidden]="!(showPlannedVolPercent$ | async)">
          Planned Percent Volume
        </div>

        <!-- Header Cell for Actual Volume Percent -->
        <div class="header-cell-1" 
             [class.hidden]="!(showActualVolPercent$ | async)">
          Actual Volume Percent
        </div>

        <!-- Header Cell for Percent Effective Percent Volume -->
        <div class="header-cell-1" 
             [class.hidden]="!(showPercentEffectiveVol$ | async)">
          Percent Effective Volume
        </div>

        <!-- Header Cell for Planned Gross Volume -->
        <div class="header-cell-1" 
             [class.hidden]="!(showPlannedVol$ | async)">
          Planned Gross Volume
        </div>

        <!-- Header Cell for Actual Gross Volume -->
        <div class="header-cell-1" 
             [class.hidden]="!(showActualVol$ | async)">
          Actual Gross Volume
        </div>
      </div>

      <!-- Content Section -->
      <ng-container *ngFor="let day of dailySummaries$ | async; let lastRow = last">
        <div class="tableRow" [class.leftShadow]="isScrolled" (scroll)="scroll($event)">
          
          <!-- Planned Percent Volume -->
          <div class="content-cell-1" 
               [attr.qa-test-id]="'PlannedPercentVolume'" 
               [class.hidden]="!(showPlannedVolPercent$ | async)">
            {{ day.summaryData?.plannedPercentVolume | number: '1.2-2' }}
          </div>

          <!-- Actual Volume Percent -->
          <div class="content-cell-1" 
               [attr.qa-test-id]="'ActualVolumePercent'" 
               [class.hidden]="!(showActualVolPercent$ | async)">
            {{ day.summaryData?.actualVolumePercent | number: '1.2-2' }}
          </div>

          <!-- Percent Effective Percent Volume -->
          <div class="content-cell-1" 
               [attr.qa-test-id]="'PercentEffectiveVolume'" 
               [class.hidden]="!(showPercentEffectiveVol$ | async)">
            {{ day.summaryData?.percentEffectiveVolume | number: '1.2-2' }}
          </div>

          <!-- Planned Gross Volume -->
          <div class="content-cell-1" 
               [attr.qa-test-id]="'PlannedGrossVolume'" 
               [class.hidden]="!(showPlannedVol$ | async)">
            {{ day.summaryData?.plannedGrossVolume | number: '1.2-2' }}
          </div>

          <!-- Actual Gross Volume -->
          <div class="content-cell-1" 
               [attr.qa-test-id]="'ActualGrossVolume'" 
               [class.hidden]="!(showActualVol$ | async)">
            {{ day.summaryData?.actualGrossVolume | number: '1.2-2' }}
          </div>
        </div>
      </ng-container>
    </div>
  </div>
</div>